#!/usr/bin/env bash

set -euo pipefail

readonly BUILD_DIR=$1
readonly COMPACT_FILES=.slug-compact

if [[ ! -n "${BUILD_DIR}" ]]; then
  echo "No build directory specified - exiting"
  exit 1
fi

cd ${BUILD_DIR}

cat COMPACT_FILES | xargs tar cpf compact.tar

cat COMPACT_FILES | xargs rm -R
